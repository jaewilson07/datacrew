name: postdown_github
on: 
  workflow_dispatch:

jobs:
  install_postdown:
    name: Install Postdown
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
      with:
        python-version: '3.10' 
    - run: pip install postdown
